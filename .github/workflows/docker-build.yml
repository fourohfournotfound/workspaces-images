name: Docker Build and Push

on:
  push:
    branches:
      - release/1.15.0  # Trigger builds on pushes to the release/1.15.0 branch
      - develop         # Trigger builds on pushes to the develop branch
    tags:
      - 'v*'            # Trigger builds on versioned tags (e.g., v1.0.0)
  pull_request:
    branches:
      - release/1.15.0  # Trigger builds for pull requests targeting the release/1.15.0 branch
  schedule:
    - cron: "0 0 * * *" # Run daily at midnight (UTC)

jobs:
  build-rolling:
    runs-on: ubuntu-latest
    environment: workspaces-images

    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Sanitize ref name (replace '/' with '-')
      - name: Sanitize ref name
        id: sanitize
        run: echo "SANITIZED_REF=$(echo ${GITHUB_REF_NAME} | tr '/' '-')" >> $GITHUB_ENV

      # Step 3: Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      # Step 4: Pull the rolling base image
      - name: Pull rolling base image
        run: docker pull kasmweb/core-ubuntu-jammy:1.15.0-rolling

      # Step 5: Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Step 6: Build and push Docker image (rolling)
      - name: Build and push Docker image (rolling)
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./dockerfile-ubuntu-jammy-cuda-rolling
          push: true
          no-cache: true
          tags: |
            variable29/desktop-ml:rolling
            variable29/desktop-ml:${{ env.SANITIZED_REF }}-rolling
          build-args: |
            BASE_IMAGE=kasmweb/core-ubuntu-jammy:1.15.0-rolling

      # Step 7: Verify the image (optional)
      - name: Verify Docker image (rolling)
        run: |
          docker pull variable29/desktop-ml:rolling
          docker run --rm variable29/desktop-ml:rolling nvcc --version || echo "CUDA not installed properly"

  build-stable:
    runs-on: ubuntu-latest
    environment: workspaces-images

    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Sanitize ref name (replace '/' with '-')
      - name: Sanitize ref name
        id: sanitize
        run: echo "SANITIZED_REF=$(echo ${GITHUB_REF_NAME} | tr '/' '-')" >> $GITHUB_ENV

      # Step 3: Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      # Step 4: Pull the stable base image
      - name: Pull stable base image
        run: docker pull kasmweb/core-ubuntu-jammy:1.15.0

      # Step 5: Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Step 6: Build and push Docker image (stable)
      - name: Build and push Docker image (stable)
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./dockerfile-ubuntu-jammy-cuda
          push: true
          no-cache: true
          tags: |
            variable29/desktop-ml:stable
            variable29/desktop-ml:${{ env.SANITIZED_REF }}-stable
          build-args: |
            BASE_IMAGE=kasmweb/core-ubuntu-jammy:1.15.0

      # Step 7: Verify the image (stable)
      - name: Verify Docker image (stable)
        run: |
          docker pull variable29/desktop-ml:stable
          docker run --rm variable29/desktop-ml:stable nvcc --version || echo "CUDA not installed properly"
